# __contains__(self, x)     x in y          Vrátí True pokud se x nachází v kolekci y.
# __delitem__(self, k)      del y[k]        Vymaže prvek na pozici k posloupnosti y.
# __getitem__(self, k)      y[k]            Vrací prvek na pozici k z posloupnosti y.
# __iter__(self)            for x in y: pass    Vrací iterátor pro prvky posl. y.
# __len__(self)             len(y)          Vrací počet prvků v y.
# __reversed__(self)        reversed(y)     Vrací zpětný iterátor pro prvky posl. y.
# __setitem__(self, k, v)   y[k] = v        Nastaví prvek na pozici k posl. y. 